#include "../main.h"
#include <stdio.h>

int main() {
    // printf("%lf\n", calculation(str));
    // validation(str);
    // printf("%s\n", str);   
    char str[256] = "1-76+x";
    // printf("%s\n", str);
    // validation_x(str, -5);
    // validation(str, -5);
    // printf("%s\n", str);
    printf("%lf\n", calculation(str, -75));
}
