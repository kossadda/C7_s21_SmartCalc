#include "../main.h"
#include <stdio.h>

int main() {
    // printf("%lf\n", calculation(str));
    // validation(str);
    // printf("%s\n", str);   
    char str[256] = "292.3391417/2^2^2";
    // printf("%s\n", str);
    // validation(str);
    // printf("%s\n", str);
    printf("%lf\n", calculation(str));
}