#include "../main.h"
#include <stdio.h>

int main() {
    // printf("%lf\n", calculation(str));
    // validation(str);
    // printf("%s\n", str);   
    char str[256] = "5+6";
    // printf("%s\n", str);
    // validation_x(str, -13.123);
    // validation(str, -5);
    // printf("%s\n", str);
    printf("%lf\n", calculation(str, -13.123));
}
